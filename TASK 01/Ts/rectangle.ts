function calculateRectangleArea(length: number, width: number): number {
  return length * width;
}

// Contoh penggunaan
const length: number = 5;
const width: number = 3;
console.log(calculateRectangleArea(length, width)); // Output: 15